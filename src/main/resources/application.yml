spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springboot?userSSL=false$useUnicode=true
    username: root
    password: 123456
  messages:
    basename: i18n/login
    # thymeleaf 中的#{} 就是用来取国际化内容的
  thymeleaf:
    cache: false